import { StylePipePipe } from './style-pipe.pipe';

describe('StylePipePipe', () => {
  it('create an instance', () => {
    const pipe = new StylePipePipe();
    expect(pipe).toBeTruthy();
  });
});
